---
interface Props {
	title: string;
}

const { title } = Astro.props;

// FONTS
import '@fontsource/manjari';
import '@fontsource/lemon';
import Navbar from '@components/Navbar.astro';
import Footer from '@components/Footer.astro';

---

<!doctype html>
<html lang="es" data-theme="light">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="¡Hola, soy Nehuen Cuenca y te doy la bienvenida a mi portfolio de desarrollador web donde describo mi formacion, proyectos, tecnologias y servicios que puedo ofrecer!" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body>
		<header>
			<Navbar />
		</header>
		<slot />
		<Footer />
	</body>
</html>
<style is:global>
	:root {
		--display-font: 'Lemon', Tahoma;
		--default-font: 'Manjari', sans-serif, Arial;
		--black: #000000;
		--white: #FFFFFF;

		--aqua-green-dark: #003C43;
		--aqua-green-dark-soft: #135D66;
		--aqua-green-mid-light: #77B0AA;
		--aqua-green-light: #E3FEF7;
	}

	[data-theme="light"] {
		--color-bg: var(--aqua-green-light);
		--color-font: var(--aqua-green-dark-soft);
		--color-titles: var(--aqua-green-dark);
		--color-details: var(--aqua-green-mid-light);
		--color-shadow: rgba(0,0,0, 0.4);
		--navbar-fade-bg: linear-gradient(180deg, var(--aqua-green-dark) 0%, var(--aqua-green-mid-light) 35%, var(--aqua-green-light) 100%);
		--projects-fade-bg: linear-gradient(180deg, rgba(119,176,170,1) 95%, rgba(227,254,247,1) 100%);
		background-color: var(--color-bg);
		color: var(--color-font);
	}

	[data-theme="dark"] {
		--color-bg: var(--aqua-green-dark);
		--color-font: var(--aqua-green-mid-light);
		--color-titles: var(--aqua-green-light);
		--color-details: var(--aqua-green-dark-soft);
		--color-shadow: transparent;
		--navbar-fade-bg: linear-gradient(180deg, var(--aqua-green-light) 0%, var(--aqua-green-dark-soft) 35%, var(--aqua-green-dark) 100% );
		--projects-fade-bg: linear-gradient(180deg, rgba(19,93,102,1) 95%, rgba(0,60,67,1) 100%);
		background-color: var(--color-bg);
		color: var(--color-font);
	}

	/* ===== Scrollbar CSS ===== */
  /* Firefox */
  * {
    scrollbar-width: auto;
    scrollbar-color: var(--color-font) var(--color-bg);
  }

  /* Chrome, Edge, and Safari */
  *::-webkit-scrollbar {
    width: 20px;
  }

  *::-webkit-scrollbar-track {
    background: var(--color-bg);
  }

  *::-webkit-scrollbar-thumb {
    background-color: var(--color-font);
    border-radius: 10px;
    border: 3px solid var(--color-bg);
  }

	/* RESETS */
	*, *::before, *::after {
		box-sizing: border-box;
	}
	
	* {
		margin: 0;
	}

	body {
		line-height: 1.5;
		-webkit-font-smoothing: antialiased;
	}

	img, picture, video, canvas, svg {
		display: block;
		max-width: 100%;
	}

	p {
		overflow-wrap: break-word;
	}

	a { 
		color:inherit;
		text-decoration: none;
	}

	button{
		background-color: transparent;
		border: none;
		cursor: pointer;
	}
	
	ul,ol { 
		list-style-type: none;
		padding: 0
	}

	html {
		font-family: var(--default-font);
		width: 100%;
		scroll-behavior: smooth;
	}

	header {
		width: 100%;
		position: sticky;
		top: 0;
		z-index: 9;
	}

	section {
		scroll-margin-block-start: 120px; /*Adds margin to the top of the viewport*/
		scroll-margin-block-end: 120px; /*Adds margin to the bottom of the viewport*/
	}

	dialog[open] {
		margin: auto;
		color: var(--color-font);
    	background-color: var(--color-bg);
    	border: 5px solid var(--color-titles);
    	border-radius: 10px;
	}

	.modal__content{
		font: normal normal 400 1.3rem var(--default-font);
	}

	.modal__content h3{
		font: normal normal 400 1.5rem var(--display-font);
		color: var(--color-titles)
	}

	.modal__content h3, .modal__content a {
		text-decoration: underline;
	}

	.modal__close, .modal__close button{
		font: normal normal 400 1.3rem var(--display-font);
		background-color: var(--color-details) !important;
		color: var(--color-titles) !important;
	}
</style>
