---
import { Icon } from "astro-icon/components";
import { Image } from "astro:assets";

import dev_ilustration from '../assets/ilustrations/dev-ilustration.png'
import ContactForm from "@components/ContactForm.vue";

---

<section class="contact-container" id="contact-section">
    <div class="contact-container__divider">
        <svg data-name="Layer 1" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1200 120" preserveAspectRatio="none">
            <path d="M1200,0H0V120H281.94C572.9,116.24,602.45,3.86,602.45,3.86h0S632,116.24,923,120h277Z" class="shape-fill"></path>
        </svg>
    </div>

    <h1 class="contact-container__title">¡Contáctame!</h1>
    <div class="content-container">
        <ContactForm client:load/>
        <div class="ways-to-contact">
            <h2 class="ways-to-contact__title">Otras vías <br> de contacto</h2>
            <h3 class="ways-to-contact__subtitle">También podés <br> hablar conmigo por acá</h3>
            <ul class="contact-ways-list">
                <li class="contact-ways-list-item">
                    <Icon class="contact-ways-list-item__way-icon" name="bx:mail-send" size={40} />
                    <span class="contact-ways-list-item__way-name">Email</span>
                    <a href="mailto:nehuen.cuenca@yahoo.com" class="contact-ways-list-item__way-keyword">nehuen.cuenca@yahoo.com</a>
                </li>
                <li class="contact-ways-list-item">
                    <Icon class="contact-ways-list-item__way-icon" name="mdi:linkedin" size={40} />
                    <span class="contact-ways-list-item__way-name">LinkedIn</span>
                    <a href="https://www.linkedin.com/in/nehuen-cuenca/" target="_blank" class="contact-ways-list-item__way-keyword">Nehuen Cuenca</a>
                </li>
            </ul>
            <Image src={dev_ilustration} alt="Ilustracion de un programador saludando desde su escritorio" class="ways-to-contact__dev-image">
        </div>
    </div>
</section>

<style>
    .contact-container{
        background-color: var(--color-details);
        color: var(--color-titles);
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 2rem;
        padding-bottom: 5rem;
    }

    .contact-container__divider {
        width: 100%;
        overflow: hidden;
        line-height: 0;
        margin-bottom: 2rem;
    }

    .contact-container__divider svg {
        position: relative;
        display: block;
        width: calc(100% + 1.3px);
        height: clamp(70px,10vh,200px);
    }

    .contact-container__divider .shape-fill {
        fill: var(--color-bg);
    }

    .contact-container__title{
        font: normal normal 600 clamp(2.5rem, 10vw, 4.5rem) var(--display-font, Tahoma);
    }

    .content-container{
        width: 90%;
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
        gap: 4rem 2rem;
    }

    .ways-to-contact{
        width: clamp(300px, 100%, 40vw);
        /* padding: 0 3rem; */
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1.5rem;
    }
    .ways-to-contact__title{
        font: normal normal 400 clamp(1.8rem, 10vw, 2.2rem) var(--display-font, Tahoma);
    }
    .ways-to-contact__title,
    .ways-to-contact__subtitle{
        text-align: center;
    }

    .ways-to-contact__subtitle{
        text-wrap: balance;
        font: normal normal 200 clamp(1.5rem, 8vw, 1.8rem) var(--default-font, Arial);
        color: var(--color-font)
    }

    .contact-ways-list{
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }
    .contact-ways-list-item{
        display: grid;
        grid-template-areas: 
            "icon name"
            "icon keyword"
        ;
        grid-template-columns: 40px 1fr;
        gap: .5rem;
    }
    .contact-ways-list-item__way-icon{
        grid-area: icon;
    }
    .contact-ways-list-item__way-icon,
    .contact-ways-list-item__way-name{
        color: var(--color-titles);
        align-self: center;
    }
    .contact-ways-list-item__way-name{
        grid-area: name;
        font: normal normal 200 clamp(1.5rem, 6vw, 1.7rem) var(--display-font, Tahoma);
    }
    .contact-ways-list-item__way-keyword{
        grid-area: keyword;
        font: normal normal 200 clamp(1.3rem, 6vw, 1.5rem) var(--default-font, Arial);
        color: var(--color-font);
    }

    .ways-to-contact__dev-image{
        object-fit: contain;
        height: 45vh;
    }

    @media (width >= 768px){
        .content-container{
            flex-direction: row-reverse;
        }

        .contact-container__divider svg {
            height: clamp(80px, 15vh, 140px);
        }

        .ways-to-contact__dev-image{
            height: 25vh;
        }
    }
    
    @media (width >= 1024px){
        .ways-to-contact__dev-image{
            height: 45vh;
        }
    }
</style>
